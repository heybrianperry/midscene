<rule_activation id="fb0b8fe4-3ec3-46ef-bef5-cb79899d3a20" title="Adopt Client-Side Cache Layer for Public API State Management: Cache Layers Implement" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files matching public API client implementations, visualizer, playground, and blackboard component code.
</rule_activation>

### Rules

- **R-CACHE-001** MAY: Cache layers MAY implement background synchronization to refresh stale data proactively.

### Verify

```bash
# Verify IndexedDB, localStorage, or sessionStorage usage patterns in cache/storage contexts
grep -r 'IndexedDB\|localStorage\|sessionStorage' packages/*/src --include='*.ts' --include='*.tsx' | grep -i 'cache\|storage\|provider'

# Verify cache TTL and expiration logic implementation
grep -r 'cache.*TTL\|time.*to.*live\|expir' packages/*/src --include='*.ts' --include='*.tsx'

# Verify storage provider abstractions and cache interfaces
grep -r 'class.*StorageProvider\|interface.*Cache' packages/*/src --include='*.ts' --include='*.tsx'
```

**Accept when:**
- All public API client implementations include a documented cache layer using browser storage APIs (IndexedDB, localStorage, or sessionStorage)
- Cache implementations include explicit TTL configuration and invalidation logic with metadata (timestamp, version, TTL)
- Storage provider abstractions exist to decouple cache implementation from consumer code
- Cache keys follow consistent naming convention: 'api:version:endpoint:params'
- Developer tools or debug modes are available to inspect, clear, and monitor cache state

<enforcement>
Claude Code MUST verify cache layer presence and TTL/invalidation logic in all public API client implementations. Violations must be flagged for revision. Exceptions require approval from both API Architecture Team and Security Team.
</enforcement>