<rule_activation id="abf5958a-9f82-45cb-81ae-ca46a8488b4c" title="Standardize Unit Test Structure with Vitest for TypeScript Packages: Unit Tests Organized" applies_to="**/*.ts">
These rules are ALWAYS ACTIVE for all TypeScript packages within the monorepo. Unit tests must follow a standardized structure using Vitest.
</rule_activation>

### Rules

- **R-VITEST-001** MUST: Unit tests MUST be organized in a tests/unit or tests/unit-test directory within each package

### Verify

```bash
# Verify unit tests exist in correct directory structure
find packages -name '*.test.ts' -type f | grep -E 'tests/(unit|unit-test)/' | wc -l

# Verify Vitest imports are present in test files
grep -r "from 'vitest'" packages/*/tests/unit*/*.test.ts | wc -l

# Verify vitest.config files exist in package roots
find packages -name 'vitest.config.ts' -o -name 'vitest.config.js' | wc -l
```

**Accept when:**
- All TypeScript packages contain unit tests in tests/unit or tests/unit-test directories with .test.ts extensions
- Vitest imports are present in test files and vitest.config files exist in package roots
- CI/CD pipeline successfully executes test commands and reports results for all packages
- Test coverage reports are generated and meet minimum threshold requirements

<enforcement>
Claude Code MUST NOT skip or defer verification. All TypeScript packages must comply with R-VITEST-001. Violations result in CI/CD pipeline failure, PR blocking, and automated non-compliance notifications.
</enforcement>