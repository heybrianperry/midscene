<rule_activation id="3d0f0be5-209d-4b79-ac0d-db63d674723f" title="Standardize External API Boundaries with Service Definition Patterns: External Not Expose" applies_to="**/*">
These rules are ALWAYS ACTIVE for all external API implementations and service boundary definitions across the codebase.
</rule_activation>

### Rules

- **R-EXT-001** MUST_NOT: External APIs MUST NOT expose internal implementation types, classes, or data structures directly without abstraction

### Verify

```bash
# Count exported providers across packages
grep -r "export.*Provider" packages/*/src --include="*.ts" --include="*.tsx" | grep -v "test" | wc -l

# Find provider and service definitions with interfaces or abstract classes
find packages -name "*-provider.ts" -o -name "*-service.ts" | xargs grep -l "interface\|abstract class" | wc -l

# Check TypeScript compilation errors
npm run type-check 2>&1 | grep -c "error TS"
```

**Accept when:**
- All external APIs have corresponding TypeScript interface or abstract class definitions
- Provider pattern implementations exist for storage and platform abstractions as evidenced by *-provider.ts files
- Type checking passes without errors related to API contract violations
- Public API surfaces are documented with clear boundaries between public and internal concerns

<enforcement>
Claude Code MUST verify all rules in this file during code review and MUST NOT skip or defer verification of external API boundaries. TypeScript compiler strict mode checks during CI build process are mandatory. Code review checklist requires explicit approval for any public API changes. Automated linting rules must detect exports without proper interface definitions. API documentation generation must fail if public methods lack JSDoc comments. Violations result in CI build failure and require architecture review board approval.
</enforcement>