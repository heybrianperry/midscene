<rule_activation id="d8ca64ff-9536-4716-9a02-3eb2359a718c" title="Standardize Core Library Module Imports and Dependencies: Core Library Versions" applies_to="**/*">
These rules are ALWAYS ACTIVE for all TypeScript, JavaScript, C++, and manifest files across packages/visualizer, packages/recorder, packages/cli, apps/*, and packages/computer/native/.
</rule_activation>

### Rules

- **R-20-001** SHOULD: Core library versions SHOULD be managed centrally through workspace-level dependency management (e.g., pnpm workspace, yarn workspaces).
- **R-20-006** SHOULD: Import ordering and module organization SHOULD follow consistent patterns across all file types (TypeScript, JavaScript, C++).

### Verify

```bash
# Verify ESLint import ordering for TypeScript/JavaScript files
eslint --ext .ts,.tsx,.js,.mjs --rule 'import/order: error' packages/ apps/

# Check import statements across visualizer, recorder, and apps
grep -r "^import.*from" packages/visualizer/src packages/recorder/src apps/ | head -20

# Verify all package.json files declare core library dependencies explicitly
find packages/ apps/ -name 'package.json' -exec jq '.dependencies, .devDependencies' {} \;

# Detect circular dependencies
madge --circular --extensions ts,tsx,js packages/ apps/
```

**Accept when:**
- ESLint import ordering rules pass for all TypeScript/JavaScript files with no violations
- No circular dependencies detected by madge or similar dependency analysis tools
- All package.json files declare core library dependencies explicitly with no implicit peer dependencies
- Code review checklist includes verification of import ordering and dependency declarations
- All C++ native module files use consistent include ordering via clang-format or similar tooling
- Pre-commit hooks validate import ordering and flag circular dependencies

<enforcement>
Claude Code MUST NOT skip or defer verification. All verify commands MUST pass before accepting changes. Violations block CI builds and pull requests. Exceptions require tech lead review and documentation with ticket references.
</enforcement>