<rule_activation id="f29778c3-0c85-40f1-902b-c4dcafc466d5" title="Standardize Unit Test Structure with Vitest for TypeScript Packages: Test Suites Use" applies_to="**/*.test.ts">
These rules are ALWAYS ACTIVE for all TypeScript unit test files within the monorepo. Test suites SHOULD use describe blocks to group related tests and it/test blocks for individual test cases.
</rule_activation>

### Rules

- **R-VITEST-001** SHOULD: Test suites SHOULD use describe blocks to group related tests and it/test blocks for individual test cases.

### Verify

```bash
# Verify unit test files exist in correct directory structure
find packages -name '*.test.ts' -type f | grep -E 'tests/(unit|unit-test)/' | wc -l

# Verify Vitest imports are present in test files
grep -r "from 'vitest'" packages/*/tests/unit*/*.test.ts | wc -l

# Verify vitest.config files exist in package roots
find packages -name 'vitest.config.ts' -o -name 'vitest.config.js' | wc -l
```

**Accept when:**
- All TypeScript packages contain unit tests in tests/unit or tests/unit-test directories with .test.ts extensions
- Vitest imports are present in test files and vitest.config files exist in package roots
- CI/CD pipeline successfully executes test commands and reports results for all packages
- Test coverage reports are generated and meet minimum threshold requirements (e.g., 80% line coverage)
- Test files use describe blocks to group related tests and it/test blocks for individual test cases

<enforcement>
Claude Code MUST verify test structure compliance before accepting TypeScript unit test files. Verification is mandatory and MUST NOT be skipped or deferred.
</enforcement>