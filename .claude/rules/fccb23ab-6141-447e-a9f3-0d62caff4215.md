<rule_activation id="fccb23ab-6141-447e-a9f3-0d62caff4215" title="Standardize External API Boundaries with Service Definition Patterns: Service Boundaries Clearly" applies_to="**/*">
These rules are ALWAYS ACTIVE for all external API implementations and service boundary definitions across the codebase.
</rule_activation>

### Rules

- **R-SVC-001** MUST: Service boundaries MUST clearly separate public API surfaces from internal implementation details.

### Verify

```bash
# Count exported providers across packages
grep -r "export.*Provider" packages/*/src --include="*.ts" --include="*.tsx" | grep -v "test" | wc -l

# Find provider and service definitions with interfaces or abstract classes
find packages -name "*-provider.ts" -o -name "*-service.ts" | xargs grep -l "interface\|abstract class" | wc -l

# Check TypeScript compilation for contract violations
npm run type-check 2>&1 | grep -c "error TS"
```

**Accept when:**
- All external APIs have corresponding TypeScript interface or abstract class definitions
- Provider pattern implementations exist for storage and platform abstractions as evidenced by *-provider.ts files
- Type checking passes without errors related to API contract violations
- Public API surfaces are documented with clear boundaries between public and internal concerns

<enforcement>
Claude Code MUST NOT skip or defer verification. TypeScript compiler strict mode checks during CI build process are mandatory. Code review checklist requiring explicit approval for any public API changes is mandatory. Automated linting rules detecting exports without proper interface definitions are mandatory. API documentation generation that fails if public methods lack JSDoc comments is mandatory.
</enforcement>