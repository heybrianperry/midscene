<rule_activation id="88aedbbd-8228-400a-ae0f-d9cda525ad60" title="Adopt Event-Driven Polling with Interval-Based State Synchronization: Polling Intervals Stored" applies_to="**/*">
These rules are ALWAYS ACTIVE for all data access patterns involving periodic state synchronization, event optimization, and keepalive mechanisms across the codebase, including browser extension background scripts, real-time streaming components, and event optimization utilities.
</rule_activation>

### Rules

- **R-POLL-001** MUST: Polling intervals MUST be stored as constants or configuration values rather than hardcoded magic numbers.

### Verify

```bash
# Count setInterval/setTimeout calls without corresponding cleanup
grep -r 'setInterval\|setTimeout' --include='*.ts' --include='*.js' | grep -v 'clearInterval\|clearTimeout' | wc -l

# Verify setInterval calls have corresponding clearInterval
grep -r 'setInterval' --include='*.ts' --include='*.tsx' -A 10 | grep -c 'clearInterval'

# Check for polling/interval/keepalive tests with cleanup verification
npm run test -- --grep 'polling\|interval\|keepalive' --reporter json | jq '.tests[] | select(.title | contains("cleanup"))'
```

**Accept when:**
- All setInterval and setTimeout calls have corresponding clearInterval/clearTimeout in cleanup paths (component unmount, error handlers, connection close)
- Polling intervals are defined as named constants or configuration values rather than inline magic numbers
- Unit tests exist for polling mechanisms that verify cleanup behavior using fake timers (jest.useFakeTimers or sinon.useFakeTimers)

<enforcement>
Claude Code MUST NOT skip or defer verification. ESLint custom rules, code review checklists, automated memory leak detection tests, and static analysis tools are mandatory enforcement mechanisms. Violations block CI pipeline and code review merge until remediated.
</enforcement>